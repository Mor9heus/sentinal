<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sentinal.app">

    <!-- Location for geofences -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- For foreground service + notifications -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Re-register geofences after reboot / app updates -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="Sentinal"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Sentinal">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground sentinel (impact/water + watch status) -->
        <service
            android:name=".service.GuardianService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Geofence transition receiver -->
        <receiver
            android:name=".geofence.GeofenceReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Re-register geofences on boot and app update -->
        <receiver
            android:name=".geofence.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
